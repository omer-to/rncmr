**_Core Component: AcitivityIndicator_**

Takes all the same props as View, plus a few more:

_props_

    animating: true | false (whether to show or hide the indicator)
    color: Color (gray is the default)
    size: 'small' | 'large'
